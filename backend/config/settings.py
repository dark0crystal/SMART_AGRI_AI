"""
Django settings for config project.

Generated by 'django-admin startproject' using Django 6.0.3.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/6.0/ref/settings/
"""

import os
from pathlib import Path

from dotenv import load_dotenv

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Load backend/.env into the process environment (optional file).
load_dotenv(BASE_DIR / ".env")


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.environ.get(
    "DJANGO_SECRET_KEY",
    "django-insecure-dv2z0$-+4s=7pk0_-(2tzidlvml71tto(xr8+0m4819$@zuk@^",
)

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.environ.get("DJANGO_DEBUG", "true").lower() in ("1", "true", "yes")

ALLOWED_HOSTS = [
    h.strip()
    for h in os.environ.get("DJANGO_ALLOWED_HOSTS", "localhost,127.0.0.1").split(",")
    if h.strip()
]


# Application definition

INSTALLED_APPS = [
    "django.contrib.staticfiles",
    "rest_framework",
    "corsheaders",
    "api",
]

MIDDLEWARE = [
    "django.middleware.security.SecurityMiddleware",
    "corsheaders.middleware.CorsMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
]

ROOT_URLCONF = 'config.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'config.wsgi.application'


# Database
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.dummy',
    }
}


# Password validation
# https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
]


# Internationalization
# https://docs.djangoproject.com/en/6.0/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/6.0/howto/static-files/

STATIC_URL = "static/"

DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"

# Firebase Admin SDK (Auth, Firestore, Storage, Realtime Database)
# https://firebase.google.com/docs/admin/setup
#
# Provide exactly one credential source:
#   FIREBASE_CREDENTIALS_PATH      — path to service account JSON file
#   FIREBASE_CREDENTIALS_JSON      — raw JSON string (e.g. single-line in env)
#   FIREBASE_CREDENTIALS_BASE64    — base64-encoded service account JSON
#
# Optional:
#   FIREBASE_PROJECT_ID             — override project id (usually inferred from JSON)
#   FIREBASE_STORAGE_BUCKET         — e.g. my-project.appspot.com
#   FIREBASE_DATABASE_URL           — Realtime Database root URL (not needed for Firestore-only)
FIREBASE_CREDENTIALS_PATH = os.environ.get("FIREBASE_CREDENTIALS_PATH", "") or None
FIREBASE_CREDENTIALS_JSON = os.environ.get("FIREBASE_CREDENTIALS_JSON", "") or None
FIREBASE_CREDENTIALS_BASE64 = os.environ.get("FIREBASE_CREDENTIALS_BASE64", "") or None
FIREBASE_PROJECT_ID = os.environ.get("FIREBASE_PROJECT_ID", "") or None
FIREBASE_STORAGE_BUCKET = os.environ.get("FIREBASE_STORAGE_BUCKET", "") or None
FIREBASE_DATABASE_URL = os.environ.get("FIREBASE_DATABASE_URL", "") or None

# Comma-separated URL prefixes; if set, image URLs for diagnoses must start with one.
ALLOWED_STORAGE_IMAGE_URL_PREFIXES = os.environ.get(
    "ALLOWED_STORAGE_IMAGE_URL_PREFIXES", ""
).strip() or None


def _resolve_under_base_dir(path: str) -> Path:
    p = Path(path)
    if p.is_absolute():
        return p
    return BASE_DIR / p


# Vision model (PyTorch checkpoint under backend/models/ by default)
VISION_MODEL_PATH = str(
    _resolve_under_base_dir(
        os.environ.get("VISION_MODEL_PATH", "models/final_model.pth"),
    )
)
VISION_MIN_CONFIDENCE = float(os.environ.get("VISION_MIN_CONFIDENCE", "0.35"))
VISION_IMAGE_MAX_BYTES = int(
    os.environ.get("VISION_IMAGE_MAX_BYTES", str(10 * 1024 * 1024))
)
VISION_IMAGE_DOWNLOAD_TIMEOUT = int(
    os.environ.get("VISION_IMAGE_DOWNLOAD_TIMEOUT", "30")
)
VISION_UNKNOWN_DISEASE_NAME_EN = os.environ.get(
    "VISION_UNKNOWN_DISEASE_NAME_EN",
    "Unknown / low confidence",
)

VISION_MODEL_NAME = os.environ.get("VISION_MODEL_NAME", "efficientnet_b1")
VISION_WITCH_BROOM_LABEL = os.environ.get("VISION_WITCH_BROOM_LABEL", "Witch's Broom")
VISION_WITCH_BROOM_MAX_DIFF = float(
    os.environ.get("VISION_WITCH_BROOM_MAX_DIFF", "0.35")
)
VISION_LOG_CLASS_PROBS = os.environ.get("VISION_LOG_CLASS_PROBS", "true").lower() in (
    "1",
    "true",
    "yes",
)
# Django REST framework
# https://www.django-rest-framework.org/api-guide/settings/

REST_FRAMEWORK = {
    "DEFAULT_AUTHENTICATION_CLASSES": [
        "api.authentication.FirebaseAuthentication",
    ],
    "DEFAULT_PERMISSION_CLASSES": [
        "rest_framework.permissions.AllowAny",
    ],
    "UNAUTHENTICATED_USER": None,
    "UNAUTHENTICATED_TOKEN": None,
    "DEFAULT_RENDERER_CLASSES": [
        "rest_framework.renderers.JSONRenderer",
    ],
    "DEFAULT_PARSER_CLASSES": [
        "rest_framework.parsers.JSONParser",
    ],
    "DEFAULT_PAGINATION_CLASS": "rest_framework.pagination.PageNumberPagination",
    "PAGE_SIZE": 20,
}

# django-cors-headers — set CORS_ALLOWED_ORIGINS in production when DEBUG is False
# https://github.com/adamchainz/django-cors-headers

if DEBUG:
    CORS_ALLOW_ALL_ORIGINS = True
else:
    CORS_ALLOWED_ORIGINS = [
        origin.strip()
        for origin in os.environ.get("CORS_ALLOWED_ORIGINS", "").split(",")
        if origin.strip()
    ]
